# -*- coding: utf-8 -*-
"""SaMST.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1AdV4VSNR0tYfT72xc6trZ3NGHQttdY7Y
"""

from google.colab import drive
drive.mount('/content/drive') # Help gg colab access to gg drive

# Install necessary lib (torch 2.0.0 is not available in python 3.12.11 so install the new one)
!pip install torch==2.3.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
!pip install numpy matplotlib opencv-python

# Check Torch and Cuda
import torch
print(torch.__version__)
print(torch.cuda.is_available())

# Commented out IPython magic to ensure Python compatibility.
# Clone the model
!git clone https://github.com/SYSU-SAIL/SaMST
# %cd SaMST

# Resize the input picture
# Post the picture to content and copy the right road
import cv2
img = cv2.imread('./content/face.jpg')
img = cv2.resize(img, (512, 512))
cv2.imwrite('./content/face.jpg', img)

# Commented out IPython magic to ensure Python compatibility.
# %cd /content/SaMST/test_model/test

# Remember to check the content folder before run, sometime it have ipynb_checkpoint when we delete some thing, it affect this code
!python test.py

import cv2
from matplotlib import pyplot as plt

# Open the output folder, copy the road of picture that you want and paste here
img = cv2.imread('/content/SaMST/outputs/style10_face.jpg')
if img is not None:
    img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
    plt.imshow(img)
    plt.axis('off')
    plt.show()
else:
    print("Can not read the file")